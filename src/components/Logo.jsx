import React from "react";

const Logo = () => {
  return (
    <nav className="py-10 px-20">
      <label htmlFor="logo" className="font-semibold text-3xl">
        Add Items to Cart
      </label>
    </nav>
  );
};

export default Logo;
